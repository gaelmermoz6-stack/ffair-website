{
  "name": "fxair-clone",
  "version": "1.0.0",
  "description": "FXAIR Private Jet Charter Website Clone",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "nodemon server/index.js",
    "client": "cd client && npm start",
    "install:all": "npm install && cd client && npm install"
  },
  "dependencies": {
    "concurrently": "^8.2.0",
    "nodemon": "^3.0.0"
  }
}
